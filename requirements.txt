# Core (CPU-only; no GPU dependencies)
anndata>=0.10
scanpy>=1.10
numpy>=1.26
scipy>=1.11
pandas>=2.1
scikit-learn>=1.4        # LASSO, OMP, linear baselines, stability selection
statsmodels>=0.14        # Fisher's exact / enrichment stats
gseapy>=1.1              # gene-set / pathway enrichment
matplotlib>=3.8
seaborn>=0.13
streamlit>=1.33          # explorer UI
requests>=2.31           # PubMed / Open Targets REST fallback
tqdm>=4.66

# Optional / stretch (kept out of the critical path)
# torch  --> only if you attempt the optional scGen-style comparison; not required.
